<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.recipeit.recipeit">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="android.support.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Accueil">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Connexion" />
        <activity android:name=".Inscription" />
        <activity android:name=".choixAvatar" />
        <activity android:name=".Accueil_Connect" />
        <activity android:name=".Espace_perso" />
        <activity android:name=".AjoutRecette" />
        <activity
            android:name=".ParameterActivity"
            android:label="@string/title_activity_parameter"
            android:theme="@style/AppTheme" />
        <activity android:name=".RechercheActivity" />
        <activity android:name=".RecetteActivity"></activity>
    </application>

</manifest>